console.log(`Install dependencies with \`npm install\`, then run:

- \`npm run db:generate -- add-table-name\` to scaffold a new SQL migration
- \`npm run db:migrate\` to apply local Postgres migrations
- \`npm run db:check\` to verify database connectivity
- \`npm run dev\` to start the web app`);

